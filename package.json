{
  "name": "parcel-static-demo",
  "version": "1.0.0",
  "description": "Simple static site demo for Parcel build testing",
  "scripts": {
    "dev": "parcel serve index.html",
    "clean": "rimraf dist .parcel-cache",
    "build": "pnpm run clean && parcel build index.html --no-optimize"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "rimraf": "^6.0.1"
  },
  "packageManager": "pnpm@10.12.1"
}
